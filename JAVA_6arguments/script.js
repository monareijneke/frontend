const piantingWalls = function (side, color) {
    console.log("The" + " " + side + "wall has been painted" + " " + color);
};

const livingroomColor = "grey";
const WallSides = "north";

piantingWalls(WallSides, livingroomColor);
